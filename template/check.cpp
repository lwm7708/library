#include <fstream>
#include <ios>
#include <iostream>

auto fin = std::ifstream("input.txt");

auto check() {

}

auto main() -> int {

    std::cin.tie(nullptr);

    std::ios_base::sync_with_stdio(false);

    check();

    return 0;

}
