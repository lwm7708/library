#include <ios>
#include <iostream>

auto solve() {

}

auto main() -> int {

    std::cin.tie(nullptr);

    std::ios_base::sync_with_stdio(false);

    auto t = 0;

    std::cin >> t;

    for (auto i = 0; i < t; ++i) {
        solve();
    }

    return 0;

}
